for task 1.2 the user is asked to type either 1 or 0 for the type of input they want
the map is printed out to show frequency of data and then the encoded keys is printed
and then the encoded message is printed and followed after it the encoded message is then decoded and printed